import {Screen} from '@components';
import React from 'react';
import {SetupAccount} from '../containers';

const OnboardingScreen = () => {
  return (
    <Screen>
      <SetupAccount />
    </Screen>
  );
};

export default OnboardingScreen;
